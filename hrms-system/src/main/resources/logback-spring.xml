<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <property name="APP_Name" value="HRMS" />
    <contextName>${APP_Name}</contextName>
    <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
    <!-- %black(%contextName-) %red(%d{yyyy-MM-dd HH:mm:ss}) %green([%thread]) %highlight(%-5level) %boldMagenta(%logger{36}) - %gray(%msg%n)-->
    <property name="PatternWithColor" value=" %black([%contextName]) %red([%d{yyyy-MM-dd HH:mm:ss}]) %highlight([%-5level]) %green([%thread]) %boldMagenta([%logger{80}-%M:%L]) - %gray(%msg%n)" />
    <property name="PatternWithoutColor" value=" [%contextName] [%d{yyyy-MM-dd HH:mm:ss}] [%-5level] [%thread] [%logger{80}-%M:%L] - %msg%n" />
    <property name="logging.files" value="../logs/${APP_Name}/${APP_Name}.log" />
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <!-- 日志输出编码 -->
        <encoder>
            <pattern>${PatternWithColor}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    <!-- 按照每天生成日志文件 -->
    <appender name="rollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logging.files}</file>
        <!-- 日志输出编码 -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志输出文件名 -->
            <fileNamePattern>${logging.files}.%d{yyyy-MM-dd}</fileNamePattern>
            <!-- 日志保存的最大时间 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${PatternWithoutColor}</pattern>
            <charset>UTF-8</charset>
        </encoder>

    </appender>

    <!-- 在程序的开发测试阶段，可以调整level的级别，常用级别优先级为： -->
    <!-- TRACE < DEBUG < INFO < WARN < ERROR  -->
    <logger name="com.sunten" level="DEBUG" />
    <!--    <logger name="com.baomidou.dynamic.datasource.DynamicRoutingDataSource" level="DEBUG" />-->

    <!--    &lt;!&ndash;log4jdbc &ndash;&gt;-->
    <logger name="jdbc.sqltiming" />
    <logger name="jdbc.resultset" />

    <!--myibatis log configure-->
    <logger name="com.apache.ibatis" level="TRACE"/>
    <logger name="java.sql.Connection" level="DEBUG"/>
    <logger name="java.sql.Statement" level="DEBUG"/>
    <logger name="java.sql.PreparedStatement" level="DEBUG"/>

    <logger name="com.atomikos.jdbc.nonxa.AtomikosNonXADataSourceBean" level="DEBUG"/>
    <logger name="com.atomikos.jdbc.AbstractDataSourceBean" level="DEBUG"/>
    <logger name="com.atomikos.icatch.imp" level="DEBUG"/>
    <logger name="org.springframework.transaction.jta.JtaTransactionManager" level="DEBUG"/>
    <logger name="com.baomidou.mybatisplus.core.metadata.TableInfoHelper" level="ERROR"/>
    <!--    <logger name="com.atomikos.icatch.imp.CompositeTransactionImp" level="DEBUG"/>-->

    <root level="INFO">
        <appender-ref ref="console" />
<!--        <appender-ref ref="rollingFile" />-->
    </root>
</configuration>
